# CI workflow folder

This folder mirrors fuller PBRP trees that include CI workflows.

You can later add workflow files to automate:
- build environment setup
- recoveryimage compilation
- artifact upload
