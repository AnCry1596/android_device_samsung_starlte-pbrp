# prebuilt folder notes

Use this folder only for device-specific prebuilt artifacts required by the recovery build.

Examples:
- kernel or dtbo variants
- firmware blobs required in recovery

Do not add prebuilts from unrelated devices/chipsets.
